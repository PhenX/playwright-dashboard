name: ai-live-e2e

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "GitHub Actions no longer runs live LLM-backed end-to-end tests in this repository."
          echo "Run the live AI suites locally when needed:"
          echo "  npm run reporter:test:integration:ai:live"
          echo "  npm run app:test:ai:live"
