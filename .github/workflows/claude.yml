name: Claude Code

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "GitHub Actions no longer runs LLM-backed automation in this repository."
          echo "Use local tooling instead."
